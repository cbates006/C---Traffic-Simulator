# C++ Traffic Simulator

A small C++ program that generates the map of a city based on user input and then simulates traffic moving around the roads and stopping at traffic lights.

## Example output

A 180x50 city with five roads on each axis.

![Sample Output](README-Images/SampleOutput)